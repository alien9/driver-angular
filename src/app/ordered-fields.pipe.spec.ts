import { OrderedFieldsPipe } from './ordered-fields.pipe';

describe('OrderedFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderedFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
